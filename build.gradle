plugins {
  id 'application'
  id 'org.openjfx.javafxplugin' version '0.0.8'
}

// >>> CAMBIAR <<< por el nombre adecuado de paquete y clase principal
mainClassName = 'ProyectoBaseJavaFX.Main'

repositories {
    mavenCentral()
}

javafx {
    version = "11.0.2"
    modules = [ 'javafx.controls', 'javafx.fxml' ]
}

// Para generar un archivo ejecutable JAR. Requiere crear la clase Launcher en el proyecto
// >>> CAMBIAR <<< por el nombre adecuado de paquete en Main-Class
jar {
    manifest {
        attributes 'Main-Class': 'ProyectoBaseJavaFX.Launcher'
    }
    from {
        configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
    }
    duplicatesStrategy = DuplicatesStrategy.EXCLUDE
}

dependencies {
    // Para que se descargue automáticamente el javadoc de JavaFX. No funciona
    // la descarga directamente desde NetBeans con las librerías de JavaFX
    compileOnly "org.openjfx:javafx-base:$javafx.version:javadoc"
    compileOnly "org.openjfx:javafx-base:$javafx.version:sources"
    compileOnly "org.openjfx:javafx-controls:$javafx.version:javadoc"
    compileOnly "org.openjfx:javafx-controls:$javafx.version:sources"
    compileOnly "org.openjfx:javafx-fxml:$javafx.version:javadoc"
    compileOnly "org.openjfx:javafx-fxml:$javafx.version:sources"
    compileOnly "org.openjfx:javafx-graphics:$javafx.version:javadoc"
    compileOnly "org.openjfx:javafx-graphics:$javafx.version:sources"
    
    // Para que el archivo ejecutable JAR sea multiplataforma
    runtimeOnly "org.openjfx:javafx-graphics:$javafx.version:win"
    runtimeOnly "org.openjfx:javafx-graphics:$javafx.version:linux"
    runtimeOnly "org.openjfx:javafx-graphics:$javafx.version:mac"
}